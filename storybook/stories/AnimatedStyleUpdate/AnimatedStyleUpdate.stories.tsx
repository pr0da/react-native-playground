import { storiesOf } from '@storybook/react-native';
import React from 'react';
import AnimatedStyleUpdateExample from '.';

storiesOf('AnimatedStyleUpdate', module).add('default', () => (
  <AnimatedStyleUpdateExample />
));
